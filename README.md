# Gigaflow vSwitch Documentation

Compile and publish the documentation like this:

```console
cd ../gigaflow-vswitch.github.io
mkdocs gh-deploy --config-file ../gigaflow-documentation/mkdocs.yml --remote-branch main
```
